language: ruby
rvm:
- 2.3.0
env:
  - RAILS_ENV=test
before_script:
  - npm install -g bower
  - bower install
  - bundle exec rake db:create
  - bundle exec rake db:migrate
script:
  - bundle exec rake
